name: E2E Tests

on:
    push:
        branches: [main]
    pull_request:

permissions:
    contents: read

jobs:
    e2e:
        uses: apermo/reusable-workflows/.github/workflows/reusable-wp-e2e.yml@v0.4.3
        with:
            wp-versions: '["latest"]'
